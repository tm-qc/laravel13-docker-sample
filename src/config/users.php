<?php

return [
    /*
    |--------------------------------------------------------------------------
    | パスワード設定
    |--------------------------------------------------------------------------
    |
    | ユーザー登録・更新時に使うパスワードの最小文字数。
    | 複数のFormRequestで使う可能性があるため、ここで管理する。
    |
    */
    'password' => [
        'min_length' => 8,
    ],

    /*
    |--------------------------------------------------------------------------
    | ユーザー一覧画面の設定
    |--------------------------------------------------------------------------
    |
    | ユーザー一覧で1ページに表示する件数。
    | 業務では表示件数を変更する可能性があるため、ServiceやRepositoryに直書きしない。
    |
    */
    'pagination' => [
        'per_page' => 3,
    ],

    /*
    |--------------------------------------------------------------------------
    | アイコン画像の設定
    |--------------------------------------------------------------------------
    |
    | ユーザーのアイコン画像アップロードで使用する設定。
    | 保存先やサイズ上限を複数箇所で使う可能性があるため、ここでまとめて管理する。
    |
    */
    'icon_image' => [
        'disk' => 'public',
        'directory' => 'users',
        'max_size_kb' => 2048,
        'mimes' => ['jpg', 'jpeg', 'png', 'webp'],
    ],

    /*
    |--------------------------------------------------------------------------
    | プロフィールの設定
    |--------------------------------------------------------------------------
    |
    | users.profile カラムの文字数上限とバリデーションを合わせるための設定。
    |
    */
    'profile' => [
        'max_length' => 500,
    ],
];
